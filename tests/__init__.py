# Archivo vacío: Marca el directorio 'tests' como un paquete Python.
# Esto permite que pytest importe módulos desde 'tests/' y facilita la organización.
# Sin este archivo, Python no reconocería 'tests' como paquete, y los imports relativos fallarían.